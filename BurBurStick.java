package BurBurStick;

public class BurBurStick {
  class uploader {
    int width = 30;
    int height = 20;
    int text = "upload file";
  }
  if (this.uploader.uploadedFile()) {
    String cracx = this.crackFile();
    if (cracx) {
      this.downloadFile(this.cracx.getFile());
      System.out.printIn("cracked succesfully");
    }
  }
}
